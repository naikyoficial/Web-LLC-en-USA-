---
// Reserva espacio para AdSense. Cuando tengas aprobación de Google, pegá aquí el script
// de AdSense y eliminá el display:none del aside.
const { height = '120px', maxWidth = '100%' } = Astro.props;
---
<aside class="ad-slot" style={`min-height:${height};max-width:${maxWidth};display:none;`} aria-label="Publicidad">
  {/* Pegá aquí tu código de AdSense */}
</aside>
